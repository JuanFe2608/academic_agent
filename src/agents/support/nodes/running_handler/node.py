"""Nodo coordinador del modo operativo — placeholder del futuro academic_agent."""

from __future__ import annotations

from agents.support.state import AgentState


def running_handler(state: AgentState) -> dict:
    """Activa el routing del modo operativo sin modificar estado."""
    return {}
