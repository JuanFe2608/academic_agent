# Integración WhatsApp

Esta carpeta debe contener solo adaptadores de proveedor o canal.

- La lógica de negocio que use WhatsApp debe vivir en `services/`.
- Los nodos de `agents/` no deben llamar este paquete directamente.
